name: Build Books ZIP

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install zip
        run: sudo apt-get update -qq && sudo apt-get install -y -qq zip

      - name: Create books ZIP
        run: |
          cd books
          zip -r ../djibril-books.zip .
          cd ..
          ls -lh djibril-books.zip

<<<<<<< HEAD
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
=======
      - name: Delete old release if exists
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete books-zip --yes 2>/dev/null || true
          git push --delete origin refs/tags/books-zip 2>/dev/null || true

      - name: Create release with ZIP
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create books-zip \
            --title "Bibliothèque Djibril — Tous les livres" \
            --notes "Archive ZIP complète de la bibliothèque (générée automatiquement à chaque push)" \
            --target main \
            djibril-books.zip
>>>>>>> e47ee71 (fix: use GitHub archive URL + Release workflow)
